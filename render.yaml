services:
  - type: web
    name: x-to-obsidian
    env: docker
    # Dockerfile 方式部署（包含 Python + Node.js + xreach）
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
      - key: NODE_VERSION
        value: 18